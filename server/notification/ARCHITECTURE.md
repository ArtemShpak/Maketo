# Notification Module - Clean Architecture

## 📐 Архитектура модуля

Модуль **notification** реализован согласно принципам **Clean Architecture (Hexagonal Architecture)**:

```
┌─────────────────────────────────────────────────────────────┐
│                  notification-microservice                   │
│           (Spring Boot Application - Main Entry)            │
└─────────────────────────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
┌───────────────┐   ┌──────────────┐   ┌──────────────────┐
│notification-api│   │notification- │   │notification-spi- │
│  (Use Cases)   │   │    core      │   │    adapter       │
│   Интерфейсы   │   │ (Business    │   │ (Реализация SPI) │
│                │   │   Logic)     │   │                  │
└───────────────┘   └──────────────┘   └──────────────────┘
        ▲                   │                   │
        │                   ▼                   ▼
        │           ┌──────────────┐   ┌──────────────────┐
        │           │notification- │   │  JavaMailSender  │
        │           │     spi      │   │   + MailConfig   │
        │           │ (Интерфейсы) │   │                  │
        └───────────┴──────────────┴───┴──────────────────┘
```

## 📦 Модули

### 1. **notification-api**
- **Назначение**: Определяет контракты (интерфейсы) для use cases
- **Зависимости**: Никаких (только DTO)
- **Содержит**:
  - `SendActivationEmailUseCase` - интерфейс для отправки email активации
  - `UserActivationDto` - DTO для передачи данных пользователя

### 2. **notification-core**
- **Назначение**: Содержит бизнес-логику (domain layer)
- **Зависимости**: 
  - `notification-api` (реализует use cases)
  - `notification-spi` (использует SPI интерфейсы)
  - `spring-boot-starter-thymeleaf` (для шаблонов email)
- **Содержит**:
  - `EmailService` - реализация отправки email через SPI
  - `TemplateService` - сервис для генерации HTML из Thymeleaf шаблонов
  - `UserMapper` - маппер для конвертации DTO

**❌ НЕ СОДЕРЖИТ**:
  - ❌ `spring-boot-starter-mail` (перенесено в adapter)
  - ❌ Прямых зависимостей от JavaMailSender

### 3. **notification-spi** (Service Provider Interface)
- **Назначение**: Определяет интерфейсы для внешних сервисов
- **Зависимости**: Никаких
- **Содержит**:
  - `EmailSender` - интерфейс для отправки email

### 4. **notification-spi-adapter**
- **Назначение**: Реализация SPI интерфейсов и адаптеры
- **Зависимости**:
  - `notification-spi` (реализует интерфейсы)
  - `notification-core` (использует бизнес-логику)
  - `notification-api` (use cases)
  - `spring-boot-starter-mail` (для JavaMailSender)
  - `common-messaging` (для RabbitMQ)
- **Содержит**:
  - `JavaMailSenderAdapter` - реализация `EmailSender` через JavaMailSender
  - `MailConfig` - конфигурация JavaMailSender бина
  - `AuthenticationListener` - RabbitMQ listener для событий регистрации
  - `RabbitAuthenticationConfig` - конфигурация RabbitMQ

### 5. **notification-microservice**
- **Назначение**: Spring Boot приложение (точка входа)
- **Зависимости**: Все предыдущие модули
- **Содержит**:
  - `NotificationApplication` - главный класс приложения
  - `application.yml` - конфигурация

## 🎯 Преимущества такой архитектуры

### ✅ Изоляция слоёв
- Бизнес-логика (`notification-core`) **не зависит** от деталей реализации
- Легко заменить JavaMailSender на другой провайдер (SendGrid, AWS SES, etc.)

### ✅ Тестируемость
- Можно тестировать `EmailService` с mock реализацией `EmailSender`
- Не нужно поднимать реальный SMTP сервер для тестов

### ✅ Гибкость
- Можно легко добавить новые реализации SPI:
  - `SendGridAdapter implements EmailSender`
  - `AwsSesAdapter implements EmailSender`
  - `MockEmailSenderAdapter implements EmailSender` (для тестов)

### ✅ Принцип инверсии зависимостей (DIP)
```
HIGH LEVEL (core) → зависит от → ABSTRACTION (spi)
                                         ↑
                                  реализует
                                         │
                    LOW LEVEL (spi-adapter)
```

## 🔧 Конфигурация

### application.yml (или application.properties)
```yaml
spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: your-email@gmail.com
    password: your-app-password
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
```

## 🚀 Как это работает

1. **RabbitMQ** получает сообщение о регистрации пользователя
2. **AuthenticationListener** (adapter) получает событие
3. Вызывается **SendActivationEmailUseCase** (API)
4. **EmailService** (core) генерирует HTML через Thymeleaf
5. **EmailService** вызывает **EmailSender.sendHtmlEmail()** (SPI)
6. **JavaMailSenderAdapter** (adapter) отправляет email через JavaMailSender

## 📝 Пример использования

### Добавление новой реализации (например, SendGrid)

1. Создать `SendGridAdapter` в `notification-spi-adapter`:
```java
@Component
@ConditionalOnProperty(name = "email.provider", havingValue = "sendgrid")
public class SendGridAdapter implements EmailSender {
    // Реализация через SendGrid API
}
```

2. Никаких изменений в `notification-core` не требуется! ✨

## 🔍 Проверка архитектуры

**Правильно** ✅:
- `core` → зависит от → `spi` (интерфейс)
- `spi-adapter` → реализует → `spi` (интерфейс)
- `spi-adapter` → использует → `core` (бизнес-логику)

**Неправильно** ❌:
- `core` → зависит от → `spi-adapter` (нарушение DIP)
- `core` → зависит от → `JavaMailSender` (нарушение изоляции)

